<%= form_with(model: pessoa, local: true) do |form| %>
  <% if pessoa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pessoa.errors.count, "error") %> prohibited this pessoa from being saved:</h2>

      <ul>
      <% pessoa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :ano_nascimento %>
    <%= form.text_field :ano_nascimento, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :sobre %>
    <%= form.text_area :sobre, class: "form-control" %>
  </div>

  <div class="actions">
    <button class="btn btn-light" type="submit">Salvar</button>
  </div>
<% end %>
